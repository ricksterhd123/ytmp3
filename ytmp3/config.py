config = {
    "filepath": "../static",
    "log_path": "../log/bot.log",
    "bot_token": "",
    "hostname": "",
    "max_duration": {
        # indexed by role id
        # fallback
        'default': 90,
    },
}
